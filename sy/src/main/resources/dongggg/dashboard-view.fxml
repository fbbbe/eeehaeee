<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<?import javafx.geometry.Pos?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="dongggg.DashboardController"
            styleClass="root-pane dashboard-root">

    <center>
        <ScrollPane fitToWidth="true"
                    hbarPolicy="NEVER"
                    vbarPolicy="AS_NEEDED"
                    styleClass="transparent-scroll-pane">
            <content>
                <VBox spacing="28"
                      alignment="TOP_CENTER"
                      styleClass="dashboard-wrapper">
                    <padding>
                        <Insets top="32" right="32" bottom="48" left="32"/>
                    </padding>

                    <children>
                        <HBox alignment="CENTER_LEFT"
                              maxWidth="760"
                              styleClass="dashboard-topbar">
                            <Button text="← 뒤로가기"
                                    onAction="#goNoteManager"
                                    styleClass="dashboard-back-button"/>
                        </HBox>

                        <!-- 헤더 -->
                        <VBox spacing="6" maxWidth="760">
                            <Label text="동그리" styleClass="dashboard-title"/>
                            <Label text="똑똑한 암기 학습, 동그리와 함께" styleClass="dashboard-subtitle"/>
                        </VBox>

                        <!-- 메인 카드 -->
                        <HBox spacing="32"
                              alignment="CENTER_LEFT"
                              maxWidth="760"
                              styleClass="dashboard-hero-card">
                            <VBox spacing="10">
                                <Label text="마스코트 레벨" styleClass="dashboard-section-label"/>
                                <Label text="Lv. 5" styleClass="dashboard-level-value"/>
                                <Label text="다음 레벨까지 234 XP" styleClass="dashboard-section-helper"/>
                                <ProgressBar progress="0.6"
                                             prefWidth="260"
                                             styleClass="dashboard-progress"/>
                            </VBox>

                            <Pane HBox.hgrow="ALWAYS"/>

                            <VBox spacing="12" alignment="CENTER">
                                <StackPane prefWidth="140" prefHeight="140"
                                           styleClass="dashboard-mascot-circle">
                                    <Label text="🐤" styleClass="dashboard-mascot-emoji"/>
                                </StackPane>
                                <Label text="동그리" styleClass="dashboard-mascot-name"/>
                            </VBox>
                        </HBox>

                        <!-- 통계 카드 -->
                        <HBox spacing="18"
                              maxWidth="760"
                              styleClass="dashboard-stats-row">
                            <VBox spacing="10"
                                  alignment="CENTER_LEFT"
                                  HBox.hgrow="ALWAYS"
                                  styleClass="dashboard-stat-card">
                                <StackPane styleClass="dashboard-stat-icon-circle">
                                    <Label text="📘" styleClass="dashboard-stat-icon"/>
                                </StackPane>
                                <Label text="12" styleClass="dashboard-stat-value"/>
                                <Label text="개념 노트" styleClass="dashboard-stat-label"/>
                            </VBox>

                            <VBox spacing="10"
                                  alignment="CENTER_LEFT"
                                  HBox.hgrow="ALWAYS"
                                  styleClass="dashboard-stat-card">
                                <StackPane styleClass="dashboard-stat-icon-circle purple">
                                    <Label text="⚡" styleClass="dashboard-stat-icon"/>
                                </StackPane>
                                <Label text="8" styleClass="dashboard-stat-value"/>
                                <Label text="시험 횟수" styleClass="dashboard-stat-label"/>
                            </VBox>

                            <VBox spacing="10"
                                  alignment="CENTER_LEFT"
                                  HBox.hgrow="ALWAYS"
                                  styleClass="dashboard-stat-card">
                                <StackPane styleClass="dashboard-stat-icon-circle green">
                                    <Label text="📈" styleClass="dashboard-stat-icon"/>
                                </StackPane>
                                <Label text="85%" styleClass="dashboard-stat-value"/>
                                <Label text="정답률" styleClass="dashboard-stat-label"/>
                            </VBox>
                        </HBox>

                        <!-- 버튼 -->
                        <VBox spacing="12" maxWidth="760">
                            <Button fx:id="noteManageButton"
                                    text="📚  노트 관리"
                                    maxWidth="Infinity"
                                    prefHeight="58"
                                    onAction="#goNoteManager"
                                    styleClass="dashboard-primary-button"/>

                            <Button fx:id="newNoteButton"
                                    text="＋  새 노트 작성"
                                    maxWidth="Infinity"
                                    prefHeight="58"
                                    onAction="#goConceptNote"
                                    styleClass="dashboard-secondary-button"/>

                            <Button fx:id="quizButton"
                                    text="📝  시험 보기"
                                    maxWidth="Infinity"
                                    prefHeight="58"
                                    onAction="#goQuiz"
                                    styleClass="dashboard-tertiary-button"/>
                        </VBox>
                    </children>
                </VBox>
            </content>
        </ScrollPane>
    </center>

</BorderPane>
